Readme:

Run the Jupyter Notebook.
-> This creates the Data file containing all the posts for the related hashtags.
-> Also creates three different graphml files.
Note: SOmetimes the querying for posts might error out, since Instagram blocks querying data/posts throught URL calls.

Gephi:
-> Using the generated graphml files plot the networks accordingly in Gephi.
-> Then create the degree distribution histograms for the networks.
